// 1 - O aluno deve OBSERVAR o algoritmo abaixo e pesquisar sobre o que cada linha faz, descrevendo a funcionalidade daquilo.
// No exemplo abaixo vou explicar em detalhes como fazer:

var valor = 50;

if(valor === 50){
    console.log("Ola mundo");
}

// Linha 5: DEFINE uma variável chamada valor e depois ATRIBUI 50 nela.
// Linha 7: Pergunta se o valor é IDÊNTICO a 50 utilizando o operador ===
// Linha 8: O console.log("Ola mundo") uma saída para o usuário a partir do console/terminal, assim o usuário pode ver uma mensagem.

// Você vai fazer o que foi feito acima só que no código abaixo, o intuito da atividade é aos poucos entender gradualmente o funcionamento do código em ritmo mais lento o funcionamento do código.

var base = 10;
var altura = 10;

area = (base * altura) / 2;

console.log(area);

// Faça o mesmo que fiz acima só que aqui:
// Obs: Não há problema nenhum pesquisar o que cada coisa faz, o propósito da atividade é justamente esse.

// Linha 16:
// Linha 17:
// Linha 19:
// Linha 21:
